import axios from 'axios';
import {RECEIVE_XSOLLA_ACCESS_TOKEN, SUCCESS, FAILED} from '../types';

export const receiveXsollaAccessToken = (userId) => {
    return async dispatch => {
        try {
            const body = JSON.stringify()
        } catch (e) {
            return e;
        }
    }
}